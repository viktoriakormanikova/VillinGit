<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.projectexodus">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".dark.maps_dark"></activity>
        <activity android:name=".dark.search_dark" />
        <activity android:name=".dark.table_dark" />
        <activity android:name=".light.search_light" />
        <activity android:name=".dark.Home_dark" />
        <activity android:name=".light.maps_light" />
        <activity android:name=".light.table_light" />
        <activity android:name=".light.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>